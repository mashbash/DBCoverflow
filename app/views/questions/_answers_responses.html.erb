<%= form_for :answer, :url => answer_response_path(answer_id) do |f| %>
  <%= f.label :content, "Comment on Answer" %>
  <%= f.text_area :content, :rows => 2 %>
  <%= f.submit "Post comment" %>
<% end %>
